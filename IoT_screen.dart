import 'package:flutter/material.dart';

class IoTScreen extends StatelessWidget {
  const IoTScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("IoTScreen"));
  }
}
